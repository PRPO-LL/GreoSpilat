server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Vue Router runs in history mode: serve index.html for any client-side
    # route so deep links and hard refreshes don't return 404.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
